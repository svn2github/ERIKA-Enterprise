#
# make_makefile.mk
#

# This file is included by the makedist makefile and specifies the
# actions that have to be taken to generate the distribution makefile



make_makefile: \
		config/conf.mk \
		ee_$(DIST) \
		config/make_makefile.mk \
		ee_$(DIST)/pkg/cfg/options.mk
	@echo makefile up2date.

MYOPTIONS= >> ee_$(DIST)/pkg/cfg/options.mk

ee_$(DIST)/pkg/cfg/options.mk: \
			config/conf.mk \
			ee_$(DIST)/pkg/cfg \
			config/make_makefile.mk
	@echo Generating ee_$(DIST)/pkg/cfg/options.mk...
	@echo \# options.mk automatically generated by make_makefile.mk $(MYOPTIONS)
	@$(foreach s, $($(DIST)), echo $(MYOPTIONS); echo ee_$(s) = $($(s)) $(MYOPTIONS);)
